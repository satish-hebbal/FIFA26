# Copy this file to .env.local and fill in your token.
# Register a free account at https://www.football-data.org to get one (no card required).
# This is read ONLY server-side in /app/api/worldcup/route.ts and is never shipped
# to the browser, so do NOT prefix it with NEXT_PUBLIC_.
FOOTBALL_DATA_TOKEN=your_token_here
